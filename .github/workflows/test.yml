name: Test

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
          - os: ubuntu-24.04-arm
          - os: ubuntu-24.04-arm
            android: true
          - os: windows-latest
          - os: windows-11-arm
          - os: macos-15
          - os: macos-15-intel

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      # build.lua compiles the fetched stb headers, so every job needs a C
      # compiler: the runners have one, and lde installs its own on Windows.
      - name: Setup lde
        uses: lde-org/setup-lde@master
        with:
          version: nightly
          platform: ${{ matrix.android && 'Android' || '' }}

      - name: Run tests
        if: "!matrix.android"
        run: lde test

      # The examples are what a caller sees, so they are run here too: canvas
      # writes its three files, and the other two read the fixtures. They are
      # chained, so a failure in a shell that does not stop at one is still a
      # failed step.
      - name: Run the examples
        if: "!matrix.android"
        run: |
          lde -C examples/canvas run &&
          lde -C examples/inspect run -- ../../tests/fixtures/photo.jpg ../../tests/fixtures/animation.gif &&
          lde -C examples/convert run -- ../../tests/fixtures/photo.jpg converted.qoi

      # The termux container mounts the workspace, so lde has to be inside it and
      # the lockfile it writes has to be readable from the host
      - name: Fix workspace permissions (Android)
        if: matrix.android
        run: |
          chmod -R a+rw ${{ github.workspace }}
          cp ~/.lde/lde ${{ github.workspace }}/lde

      - name: Cache termux image (Android)
        if: matrix.android
        id: cache-android
        uses: actions/cache@v4
        with:
          path: /tmp/termux-android.tar
          key: termux-android-v1

      # clang is what compiles the stb shim
      - name: Run tests (Android)
        if: matrix.android
        run: |
          if [[ "${{ steps.cache-android.outputs.cache-hit }}" != "true" ]]; then
            docker run --privileged --platform linux/arm64 \
              --name termux-build termux/termux-docker:aarch64 \
              bash -c "apt update && apt install -y clang"
            docker commit termux-build termux-android:latest
            docker rm termux-build
            docker save termux-android:latest -o /tmp/termux-android.tar
          fi

          docker load -i /tmp/termux-android.tar

          docker run --rm --privileged --platform linux/arm64 \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            termux-android:latest \
            bash -c "/workspace/lde test"
